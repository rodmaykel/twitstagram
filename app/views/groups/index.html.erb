<% provide(:title, 'All groups') %>
<h1>All groups</h1>

<%= will_paginate %>

<ul class="groups">
  <% @groups.each do |group| %>
    <li>
      <%= link_to group.name, "/admin/groups/#{group.id}" %>
    </li>
  <% end %>
</ul>
